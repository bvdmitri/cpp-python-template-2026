name: CI

on:
  push:
    branches: [main]
  pull_request:
  workflow_dispatch:

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  # ---------------------------------------------------------------------------
  # Cross-platform / cross-architecture build + the FULL test suite.
  # TDD is enforced here: a red test fails the job and (with branch protection)
  # blocks the merge.
  # ---------------------------------------------------------------------------
  build-and-test:
    name: ${{ matrix.name }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - { name: "Linux x86_64 (gcc-14)", os: ubuntu-latest,    preset: dev, cc: gcc-14, cxx: g++-14 }
          - { name: "Linux arm64 (gcc-14)",  os: ubuntu-24.04-arm, preset: dev, cc: gcc-14, cxx: g++-14 }
          - { name: "macOS arm64 (clang)",   os: macos-14,         preset: dev }
          # MSVC does not yet expose a CMake C++26 dialect; use LLVM Clang on Windows.
          - { name: "Windows x64 (clang)",   os: windows-latest,   preset: dev, cc: clang, cxx: clang++ }
    env:
      CC: ${{ matrix.cc }}
      CXX: ${{ matrix.cxx }}
    steps:
      - uses: actions/checkout@v7

      - name: Install Ninja + CMake
        uses: lukka/get-cmake@latest

      - name: Install GCC 14 (Linux)
        if: startsWith(matrix.os, 'ubuntu')
        run: sudo apt-get update && sudo apt-get install -y gcc-14 g++-14

      - name: Select latest Xcode (macOS)
        if: runner.os == 'macOS'
        uses: maxim-lobanov/setup-xcode@v1
        with:
          xcode-version: latest-stable

      # LLVM Clang on Windows auto-detects the installed MSVC toolchain (headers,
      # libs, linker) via vswhere — no dev-shell action needed (and avoids the
      # Node 20-only msvc-dev-cmd).

      - name: Configure
        run: cmake --preset ${{ matrix.preset }}

      - name: Build
        run: cmake --build --preset ${{ matrix.preset }}

      - name: Test (full suite)
        run: ctest --preset ${{ matrix.preset }}

  # ---------------------------------------------------------------------------
  # Sanitizers (Linux/clang). ASan+UBSan and TSan run as separate lanes.
  # ---------------------------------------------------------------------------
  sanitizers:
    name: sanitizer (${{ matrix.preset }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        preset: [asan, tsan]
    env:
      CC: clang
      CXX: clang++
    steps:
      - uses: actions/checkout@v7
      - uses: lukka/get-cmake@latest
      - run: cmake --preset ${{ matrix.preset }}
      - run: cmake --build --preset ${{ matrix.preset }}
      - run: ctest --preset ${{ matrix.preset }}

  # ---------------------------------------------------------------------------
  # Coverage → Codecov (inspectable report + badge).
  # ---------------------------------------------------------------------------
  coverage:
    runs-on: ubuntu-latest
    env:
      CC: clang-18
      CXX: clang++-18
    steps:
      - uses: actions/checkout@v7
      - uses: lukka/get-cmake@latest
      - name: Install Clang + LLVM coverage tools
        run: |
          sudo apt-get update
          sudo apt-get install -y clang-18 llvm-18
          # Put llvm-cov / llvm-profdata (unversioned) on PATH for scripts/coverage.sh
          echo "/usr/lib/llvm-18/bin" >> "$GITHUB_PATH"
      - name: Build + test with coverage
        run: make coverage
      - name: Upload C++ coverage to Codecov
        uses: codecov/codecov-action@v7
        with:
          files: out/coverage/coverage.lcov
          flags: cpp
          name: cpp-ubuntu
          fail_ci_if_error: false
        env:
          CODECOV_TOKEN: ${{ secrets.CODECOV_TOKEN }}

  # ---------------------------------------------------------------------------
  # Formatting + static analysis gate.
  # ---------------------------------------------------------------------------
  lint:
    runs-on: ubuntu-latest
    env:
      CC: clang-18
      CXX: clang++-18
    steps:
      - uses: actions/checkout@v7
      - uses: lukka/get-cmake@latest
      - name: Install Clang 18
        run: sudo apt-get update && sudo apt-get install -y clang-18
      - uses: actions/setup-python@v6
        with: { python-version: "3.x" }
      - name: Generate compile database (clang, for matching clang-tidy)
        run: cmake --preset dev
      - name: Install pre-commit
        run: pip install pre-commit
      - name: Run pre-commit (clang-format, clang-tidy, etc.)
        run: pre-commit run --all-files --show-diff-on-failure
